import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#6b8f71",
    },
    secondary: {
      main: "#ecfeaa",
    },
    error: {
      main: "#f44336",
    },
  },
});

export default theme;
